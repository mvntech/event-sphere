{
  "name": "eventsphere-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end tests driving the real frontend against the real backend.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
